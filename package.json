{
  "name": "test-task",
  "version": "0.9.0",
  "description": "Test task for BeeJee",
  "author": "Petr Zenin <mr@adminoid.com>",
  "license": "MIT",
  "scripts": {
    "run-server": "php -S localhost:8080 public/index.php",
    "css": "node-sass assets/scss/style.scss -o public/ --output-style=compressed",
    "css:watch": "npm run css && node-sass assets/scss/style.scss -wo public/ --output-style=compressed"
  },
  "dependencies": {
    "bootstrap": "^4.2.1"
  },
  "devDependencies": {
    "node-sass": "^4.11.0"
  }
}
